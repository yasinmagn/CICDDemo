name: Deploy to PreProd server
on: 
     workflow_dispatch:

jobs:
  build-and-deploy:  
   uses: yasinmagn/CICDDemo/.github/workflows/deploy-stating.yml@master
   with:
    runner-name: "[self-hosted,web01-server]"
      
    
    
    
